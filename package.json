{
  "name": "pdf-image-toolkit",
  "version": "1.0.0",
  "private": true,
  "description": "PDF & Image Toolkit - Convert, merge, extract, and compress files",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "start": "concurrently --kill-others \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "render:build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "render:start": "cd backend && node server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
